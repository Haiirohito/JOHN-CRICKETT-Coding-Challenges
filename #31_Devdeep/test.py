import numpy as np

width = 7  # Assuming the width is 7 for the example

# Create arrays with continuous values
x_bottom_left = np.linspace(0, width, num=2, endpoint=False)
y_bottom_left = np.linspace(0, width, num=2, endpoint=False)

print("x_bottom_left:", x_bottom_left)
print("y_bottom_left:", y_bottom_left)